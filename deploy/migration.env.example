# Root-owned /etc/tradejournal/migration.env, mode 0600.
# Only the one-off migration helper reads this. Same endpoint as DATABASE_URL.
MIGRATION_DATABASE_URL=postgresql+psycopg://OWNER_USER:PASSWORD@DATABASE_HOST/DATABASE?sslmode=require
