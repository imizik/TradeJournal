# Dependencies
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pnp
.pnp.js

# Build / output
.next/
out/
dist/
build/
# Machine-generated by `next dev` / `next build`; regenerated automatically.
# Tracking them produced diff churn on every run and needless conflicts
# between parallel branches.
*.tsbuildinfo
next-env.d.ts

# Playwright failure artifacts (traces, screenshots) and reports.
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/.playwright/
*.log
*.out
*.err
*-dev.out
*-dev.err
*.egg-info/
*.egg

# Data
backend/data/
backend/Robinhood/

# IDE / misc
.idea/
.vscode/
*.swp
*.swo
.env
.env.local
backend/.env.tradingview
backend/credentials.json
backend/token.json
backend/pubsub-subscriber.json
.DS_Store

# Claude Code internal. Skills are shared project tooling -- a skill that only
# exists on one machine cannot help the cold agent that clones this repo -- so
# .claude/skills/ is tracked and everything else under .claude/ is not.
.claude/*
!.claude/skills/

# Installers
*.exe
